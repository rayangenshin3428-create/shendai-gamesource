# ============================================================================
# IA NARRATRICE (Mistral AI) — via la route serverless /api/chat.
#
# ▶ EN PRODUCTION (Vercel) — recommandé :
#   Crée une clé gratuite sur https://console.mistral.ai/api-keys/ (pas de
#   carte bancaire requise sur le tier gratuit) et mets-la dans Vercel :
#   Settings → Environment Variables → nom : MISTRAL_API_KEY  (reste SECRÈTE).
#   L'IA s'active automatiquement en production.
#
# ▶ EN LOCAL (optionnel, pour tester l'IA avec `vercel dev`) :
#   Crée un fichier `.env.local` avec les deux lignes ci-dessous.
#   (Avec `npm run dev` simple, /api/chat n'existe pas → le jeu utilise le mock.)
# ============================================================================

MISTRAL_API_KEY=
# VITE_AI_ENABLED=true
